name: Homelab Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, homelab]

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Verificar Docker
        run: docker --version

      - name: Pull imágenes actualizadas
        run: docker compose -f /root/monitoring/docker-compose.yml pull

      - name: Desplegar stack
        run: docker compose -f /root/monitoring/docker-compose.yml up -d --remove-orphans

      - name: Verificar contenedores activos
        run: docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"